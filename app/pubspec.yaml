name: homedeck
description: "HomeDeck — smart home control for phone, browser and a physical knob."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.3.12
  http: ^1.2.2
  signalr_netcore: ^1.4.4
  wakelock_plus: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
